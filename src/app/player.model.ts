export class Player {
  constructor (public name: string, public team: string, public notes: string, public stats: string) { }
}

export class Player2 {
  constructor (public name: string, public team: string, public notes: string, public stats: string) { }
}
